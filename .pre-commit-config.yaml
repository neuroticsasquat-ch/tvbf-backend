minimum_pre_commit_version: "3.5.0"

repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: ruff check
        entry: docker compose exec -T tvbf-backend ruff check src tests
        language: system
        pass_filenames: false
        files: \.py$

      - id: ruff-format
        name: ruff format --check
        entry: docker compose exec -T tvbf-backend ruff format --check src tests
        language: system
        pass_filenames: false
        files: \.py$

      - id: pyright
        name: pyright
        entry: docker compose exec -T tvbf-backend pyright src tests
        language: system
        pass_filenames: false
        files: \.py$

      - id: pytest
        name: pytest
        entry: docker compose exec -T tvbf-backend pytest tests/unit
        language: system
        pass_filenames: false
        files: \.py$
